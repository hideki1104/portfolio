<h1>コメント編集画面</h1>

			<%= form_for([@post,@post_comment],url:user_post_post_comment_path(@post.id)) do |f| %>
				<%= f.text_area :comment,class: "form-control",placeholder: "コメントをここに" %>
				<%= f.submit "コメントする",class: "btn btn-primary btn-lg active" %>
			<% end %>
